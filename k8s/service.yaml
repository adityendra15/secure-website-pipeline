apiVersion: v1
kind: Service
metadata:
  name: secure-website
  labels:
    app: secure-website
spec:
  type: ClusterIP
  selector:
    app: secure-website
  ports:
    - name: http
      port: 80
      targetPort: http
